syntax = "proto3";

package ipfs.core.data_structures;

import "cid.proto";

message BlockProto {
  bytes data = 1;  // Raw binary data of the block
  CIDProto cid = 2;  // Reference to a CID message defined in cid.proto
}
