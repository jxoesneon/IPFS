name: cli_dashboard
description: "A hacker-style CLI dashboard for dart_ipfs."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.5.4 <4.0.0'

dependencies:
  dart_ipfs:
    path: ../../
  dart_console: ^1.2.0
  async: ^2.11.0
  path: ^1.8.3

dev_dependencies:
  lints: ^3.0.0

dependency_overrides:
  intl: ^0.19.0
